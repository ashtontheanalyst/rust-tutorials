// Vectors are a collection, that allow you to store more than one value in a structure
// of the SAME type. Plus all these values are by each other in memory
// They're mutable, global, etc.

fn main() {
    let v: Vec<i32> = Vec::new(); // This tells rust you want to make a new vector of ints called v
    
}